spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rs_system?characterEncoding=utf-8
    driver-class-name: com.mysql.jdbc.Driver
    username: root
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: com.thoughtworks.rslist.config.MySQLCustomDialect
